module.exports = `
extend type Query {
  dbmeta: [document]
  getValueOptions(field: String, collection: String): [String]

  # NEU (einzig neu dazu): letzter/neuester Eintrag aus "metaData"
  getLastMetaData: MetaData
}

type document {
  collection: String
  field: [String]
}

# Typ für den Timestamp-Eintrag
type MetaData {
  id: ID
  executedAt: String
}
`
