# Ensure shell scripts always have Unix line endings
*.sh text eol=lf

# Ensure Dockerfiles always have Unix line endings
Dockerfile text eol=lf
dockerfile text eol=lf

# Ensure YAML files always have Unix line endings
*.yaml text eol=lf
*.yml text eol=lf

# Ensure JSON files always have Unix line endings
*.json text eol=lf

# Ensure configuration templates always have Unix line endings
*.template text eol=lf
*.conf text eol=lf
*.conf.template text eol=lf